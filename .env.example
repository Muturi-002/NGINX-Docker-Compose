# Blue/Green Deployment Configuration

# Docker Images - Image references for Blue and Green services
BLUE_IMAGE=node:18-alpine
GREEN_IMAGE=node:18-alpine

# Active Pool Configuration (blue or green) - Controls NGINX template
ACTIVE_POOL=blue

# Release Identifiers - Passed into app containers for X-Release-Id header
RELEASE_ID_BLUE=v1.0.0-blue
RELEASE_ID_GREEN=v1.0.0-green

# Application Port (optional) - Port the application should run on
PORT=3000